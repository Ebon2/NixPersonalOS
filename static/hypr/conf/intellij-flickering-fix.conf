# =====================================================
# intellij-flickering-fix.conf
# Hyprland 0.53+ — suppress_event en block syntax
# REQUIERE name = como primer campo (obligatorio)
# =====================================================

windowrule {
    name = jetbrains-no-activatefocus
    match:class = ^(jetbrains-.*)$
    suppress_event = activatefocus
}
